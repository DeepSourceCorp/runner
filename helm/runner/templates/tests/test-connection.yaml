apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runner.fullname" . }}-test-connection"
  labels:
    {{- include "runner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "runner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
