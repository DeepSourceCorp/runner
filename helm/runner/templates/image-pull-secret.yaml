{{ if not .Values.useExistingSecret }}
apiVersion: v1
kind: Secret
metadata: 
  name: {{ include "runner.fullname" . }}-image-registry
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |-
{{- $auth := printf "%s:%s" .Values.config.kubernetes.imageRegistry.username .Values.config.kubernetes.imageRegistry.password | b64enc }}
    {
      "auths": {
        "{{ .Values.config.kubernetes.imageRegistry.registryUrl }}": {
          "username": "{{ .Values.config.kubernetes.imageRegistry.username }}",
          "password": "{{ .Values.config.kubernetes.imageRegistry.password }}",
          "auth": "{{ $auth }}"
        }
      }
    }
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "runner.fullname" . }}-image-registry
  namespace: {{ .Values.config.kubernetes.namespace }}
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |-
{{- $auth := printf "%s:%s" .Values.config.kubernetes.imageRegistry.username .Values.config.kubernetes.imageRegistry.password | b64enc }}
    {
      "auths": {
        "{{ .Values.config.kubernetes.imageRegistry.registryUrl }}": {
          "username": "{{ .Values.config.kubernetes.imageRegistry.username }}",
          "password": "{{ .Values.config.kubernetes.imageRegistry.password }}",
          "auth": "{{ $auth }}"
        }
      }
    }
{{ end }}
